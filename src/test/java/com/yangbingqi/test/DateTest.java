package com.yangbingqi.test;


import java.text.SimpleDateFormat;
import java.util.Calendar;
import java.util.Date;

import org.junit.Test;

import com.yangbingqi.utils.DateUtils;

public class DateTest {
	
	/*
	 * @Test public void testGetDate() {
	 * 
	 * Calendar s = Calendar.getInstance(); s.set(2000, 0, 10); Date date =
	 * DateUtils.getDateByInitMonth(s.getTime()); SimpleDateFormat df = new
	 * SimpleDateFormat("yyyy-MM-dd HH:mm:ss"); String format = df.format(date);
	 * System.out.println(format); }
	 * 
	 * @Test public void MonthLastDay() {
	 * 
	 * Calendar s = Calendar.getInstance(); s.set(2000, 0, 10); Date monthLastDay =
	 * DateUtils.MonthLastDay(s.getTime()); SimpleDateFormat df = new
	 * SimpleDateFormat("yyyy-MM-dd HH:mm:ss"); String format =
	 * df.format(monthLastDay); System.out.println(format); }
	 * 
	 * @Test public void getAge() {
	 * 
	 * Calendar s = Calendar.getInstance(); s.set(2000, 0, 18); String age =
	 * DateUtils.getAge(s.getTime()); System.out.println(age); }
	 */
}
